package com.custom.dth.ui.features.playback

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Screen 2 - Flow 1: Playback Information Overlay.
 * Primary on-screen display (OSD) shown during live TV playback. Auto-hides after 5s.
 */
@Composable
fun PlaybackOSD(modifier: Modifier = Modifier) {
    // TODO: Implement playback info overlay (no transport controls, only metadata)
}
